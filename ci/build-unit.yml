---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: resource-source-code
  - name: resource-concourse
  - name: resource-detect-output

outputs:
  - name: resource-build-output

run:
  path: resource-concourse/ci/build_unit.sh