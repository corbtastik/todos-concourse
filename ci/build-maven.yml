---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: '8-jdk-alpine'

inputs:
  - name: resource-source-code
  - name: resource-concourse
  - name: resource-version

outputs:
  - name: resource-build-maven-output

run:
  path: resource-concourse/ci/build-maven.sh