---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: resource-build-output
  - name: resource-kit
  - name: resource-semver  

outputs:
  - name: resource-release-output

run:
  path: resource-kit/ci/release.sh        