---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: resource-build-output
  - name: resource-concourse
  - name: resource-version  

outputs:
  - name: resource-release-output

run:
  path: resource-concourse/ci/release.sh        