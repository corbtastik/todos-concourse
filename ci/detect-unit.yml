---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: resource-concourse
  - name: resource-source-code

outputs:
  - name: resource-unit-output

run:
  path: resource-concourse/ci/detect_unit.sh